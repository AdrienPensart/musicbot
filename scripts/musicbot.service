[Unit]
Description=Musicbot Server
Requires=network.target
After=network.target

[Service]
User=ubuntu
Type=simple
ExecStart=/home/ubuntu/musicbot/scripts/prod.sh
StandardOutput=journal
PrivateTmp=true
Restart=always

[Install]
WantedBy=multi-user.target
